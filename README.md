Описание реализованного метода

1. max = numbers[0] - берем первый элемент массива как начальное значение максимума
2. Проходим по всем элементам массива начиная со второго (i = 1) . Сравниваем каждый элемент с текущим максимумом
3. Если numbers[i] > max, то обновляем max = numbers[i]
